### Екзаменаційні білети з дисципліни «Тестування програмних систем та комплексів»

---
#### Білет №1
1. Поясніть сутність методології TDD (Test Driven Development). Опишіть цикл «Red-Green-Refactor».
2. Що таке фікстури (fixtures) у тестуванні? Яка різниця між `setUp` та `tearDown` у бібліотеці `unittest`?
3. Опишіть процес валідації вхідних даних. Чому це критично важливо для безпеки програмного забезпечення?
4. **Завдання:** У файлі `main.py` створіть функцію `calculate_average(numbers: list)`, яка приймає список чисел і повертає їх середнє арифметичне. Функція повинна коректно обробляти порожній список.
**Тест:** У файлі `test.py` напишіть Unit-тест для перевірки звичайного списку та порожнього списку.

---
#### Білет №2
1. Назвіть та опишіть основні рівні тестування (Unit, Integration, System, Acceptance).
2. Що таке покриття коду (Code Coverage)? Як інтерпретувати результати звіту про покриття?
3. Яка роль систем контролю версій (наприклад, Git) у процесі колективної розробки та тестування?
4. **Завдання:** У файлі `main.py` створіть функцію `is_palindrome(text: str)`, яка перевіряє, чи є рядок паліндромом (читається однаково з обох боків), ігноруючи регістр.
**Тест:** У файлі `test.py` перевірте слова-паліндроми, не паліндроми та пусті рядки.

---
#### Білет №3
1. Поясніть різницю між верифікацією та валідацією програмного забезпечення.
2. Для чого використовується конструкція `assert` у Python? Наведіть приклади використання у тестах.
3. Що таке CI/CD (Continuous Integration / Continuous Delivery) і як автоматичні тести інтегруються в цей процес?
4. **Завдання:** У файлі `main.py` напишіть функцію `factorial(n: int)`, яка обчислює факторіал числа. Додайте перевірку на від’ємні числа (має викликатись виключення).
**Тест:** У файлі `test.py` перевірте обчислення факторіалу та переконайтеся, що виключення `ValueError` піднімається для від’ємних чисел.

---
#### Білет №4
1. Опишіть життєвий цикл дефекту (Bug Life Cycle). Які статуси може мати баг-репорт?
2. Які переваги використання віртуальних середовищ (Virtual Environments) при тестуванні проектів на Python?
3. Що таке регресійне тестування і коли його необхідно проводити?
4. **Завдання:** У файлі `main.py` створіть клас `Calculator` з методами `add(a, b)` та `divide(a, b)`. Метод ділення має обробляти ділення на нуль.
**Тест:** У файлі `test.py` протестуйте обидва методи, зокрема перевірте реакцію на ділення на нуль.

---
#### Білет №5
1. Порівняйте методи тестування «Чорної скриньки» (Black Box) та «Білої скриньки» (White Box).
2. Як працює статичний аналіз коду (наприклад, SonarQube) і які типи помилок він дозволяє виявити?
3. Що таке Mock-об’єкти (імітація) і в яких випадках їх доцільно використовувати?
4. **Завдання:** У файлі `main.py` напишіть функцію `check_even(number: int) -> bool`, яка повертає `True`, якщо число парне, і `False`, якщо непарне.
**Тест:** У файлі `test.py` напишіть параметризований тест або кілька тестів для перевірки парних, непарних чисел та нуля.

---
#### Білет №6
1. Які основні принципи написання якісних Unit-тестів (FIRST: Fast, Independent, Repeatable, Self-validating, Timely)?
2. Поясніть призначення файлу `README.md` та формату Markdown у документуванні проекту.
3. Чим відрізняється димове тестування (Smoke Testing) від санітарного тестування (Sanity Testing)?
4. **Завдання:** У файлі `main.py` створіть функцію `max_of_three(a, b, c)`, яка повертає найбільше з трьох чисел.
**Тест:** У файлі `test.py` перевірте різні комбінації чисел (позитивні, негативні, однакові).

---
#### Білет №7
1. Опишіть структуру типового тест-кейсу (Test Case). Які поля є обов’язковими?
2. Як бібліотека `pytest` спрощує написання тестів у порівнянні з `unittest`?
3. Що таке інтеграційне тестування і які підходи до нього існують (Big Bang, Top-down, Bottom-up)?
4. **Завдання:** У файлі `main.py` напишіть функцію `reverse_string(s: str)`, яка повертає перевернутий рядок.
**Тест:** У файлі `test.py` перевірте роботу функції на звичайних рядках, рядках з пробілами та порожньому рядку.

---
#### Білет №8
1. Що таке позитивне та негативне тестування? Наведіть приклади для форми логіну.
2. Поясніть концепцію «Branching» (гілкування) у Git. Навіщо створювати окремі гілки для фіч та тестів?
3. Як автоматизувати запуск тестів при кожному коміті (Commit) за допомогою GitHub Actions?
4. **Завдання:** У файлі `main.py` створіть функцію `list_to_set(input_list: list)`, яка перетворює список у множину (видаляє дублікати) і повертає її.
**Тест:** У файлі `test.py` перевірте, чи дійсно дублікати зникають і чи повертається правильний тип даних.

---
#### Білет №9
1. Що таке граничні значення (Boundary Values) і чому помилки часто трапляються саме там?
2. Опишіть методи класу `unittest.TestCase`: `assertEqual`, `assertTrue`, `assertRaises`.
3. Яка роль QA-інженера в команді розробки Agile/Scrum?
4. **Завдання:** У файлі `main.py` напишіть функцію `get_grade(score: int)`, яка повертає оцінку (A, B, C, D, F) залежно від балу. Якщо бал < 0 або > 100, викликати помилку.
**Тест:** У файлі `test.py` протестуйте граничні значення (наприклад, 59 і 60, 89 і 90) та некоректні вхідні дані.

---
#### Білет №10
1. Що таке клас еквівалентності (Equivalence Partitioning) і як він допомагає оптимізувати кількість тестів?
2. Як встановити сторонні бібліотеки для тестування в Python? Опишіть роботу з `pip` та `requirements.txt`.
3. Поясніть різницю між помилкою (Error), дефектом (Defect) та відмовою (Failure).
4. **Завдання:** У файлі `main.py` створіть функцію `count_vowels(text: str)`, яка рахує кількість голосних літер у рядку.
**Тест:** У файлі `test.py` перевірте роботу функції на рядках з різною кількістю голосних, без голосних та у різному регістрі.

---
#### Білет №11
1. Що таке «Code Smell»? Наведіть приклади, які можуть бути виявлені під час рев'ю коду.
2. Як використовувати декоратор `@unittest.skip` та для чого він потрібен?
3. Опишіть процес Pull Request (PR). Чому важливо проводити Code Review перед злиттям гілок?
4. **Завдання:** У файлі `main.py` напишіть функцію `cel_to_fah(celsius)`, яка конвертує температуру з Цельсія у Фаренгейти.
**Тест:** У файлі `test.py` перевірте правильність розрахунків для 0, 100 та від’ємних значень.

---
#### Білет №12
1. Що таке автоматизоване тестування? Які його переваги та недоліки порівняно з ручним тестуванням?
2. Які інструменти IDE (наприклад, VS Code) допомагають у написанні та налагодженні тестів?
3. Поясніть принцип «Defect Clustering» (Скупчення дефектів).
4. **Завдання:** У файлі `main.py` створіть функцію `find_min(numbers: list)`, яка повертає мінімальне число зі списку.
**Тест:** У файлі `test.py` протестуйте функцію на списку з цілими та дробовими числами.

---
#### Білет №13
1. Що таке документація коду (Docstrings) і як вона допомагає у тестуванні?
2. Як перевірити, що функція викликає очікуване виключення (Exception) за допомогою `pytest` або `unittest`?
3. Що таке навантажувальне тестування (Load Testing) і чим воно відрізняється від стрес-тестування?
4. **Завдання:** У файлі `main.py` напишіть функцію `login(user, password)`, яка повертає `True`, якщо пароль дорівнює "admin123", інакше `False`.
**Тест:** У файлі `test.py` напишіть тест на успішний логін та тест на невдалий логін.

---
#### Білет №14
1. Які існують критерії початку та завершення тестування (Entry and Exit Criteria)?
2. Що таке параметризовані тести і як вони реалізуються в Python?
3. Опишіть, що таке «Flaky Tests» (ненадійні тести) і чому вони шкідливі.
4. **Завдання:** У файлі `main.py` створіть функцію `remove_spaces(text: str)`, яка видаляє всі пробіли з рядка.
**Тест:** У файлі `test.py` перевірте рядок з пробілами на початку, в кінці та всередині.

---
#### Білет №15
1. Поясніть поняття «Stub» (заглушка) у контексті модульного тестування.
2. Як організувати структуру проекту з тестами? Де зазвичай розміщується папка `tests`?
3. Що таке приймальне тестування (Acceptance Testing) і хто його проводить?
4. **Завдання:** У файлі `main.py` напишіть функцію `square_root(n)`, яка повертає квадратний корінь числа. Обробка від’ємних чисел обов’язкова.
**Тест:** У файлі `test.py` перевірте коректність обчислення та реакцію на від’ємне число.

---
#### Білет №16
1. Що таке «Traceability Matrix» (Матриця трасування) вимог і для чого вона використовується?
2. Як запустити всі тести у проекті з командного рядка? Опишіть команду.
3. Поясніть різницю між статичним та динамічним тестуванням.
4. **Завдання:** У файлі `main.py` створіть функцію `dict_merge(d1, d2)`, яка об’єднує два словники в один.
**Тест:** У файлі `test.py` перевірте правильність об’єднання ключів та значень.

---
#### Білет №17
1. Що таке альфа- та бета-тестування? У чому їх принципова різниця?
2. Як використовувати модуль `logging` для дебагінгу тестів замість `print`?
3. Які основні метрики якості програмного забезпечення ви знаєте?
4. **Завдання:** У файлі `main.py` напишіть функцію `is_adult(age: int)`, яка повертає `True`, якщо вік >= 18.
**Тест:** У файлі `test.py` перевірте значення 17, 18 та 19.

---
#### Білет №18
1. Що таке End-to-End (E2E) тестування? Наведіть приклад сценарію для інтернет-магазину.
2. Як перевірити, чи тест покриває певні гілки коду (Branch Coverage)?
3. Що таке «Technical Debt» (Технічний борг) і як відсутність тестів впливає на нього?
4. **Завдання:** У файлі `main.py` створіть функцію `capitalize_words(sentence: str)`, яка робить першу літеру кожного слова великою.
**Тест:** У файлі `test.py` перевірте речення з одним словом, кількома словами та порожнє речення.

---
#### Білет №19
1. Поясніть принцип «Pesticide Paradox» у тестуванні. Як з ним боротися?
2. Що таке setUpClass та tearDownClass? Чим вони відрізняються від звичайних setUp/tearDown?
3. Як працює система Continuous Integration (CI) на прикладі GitHub Actions (файл `.yml`)?
4. **Завдання:** У файлі `main.py` напишіть функцію `calc_power(base, exp)`, яка підносить число до степеня.
**Тест:** У файлі `test.py` перевірте піднесення до степеня 0, 1 та 2.

---
#### Білет №20
1. Опишіть структуру звіту про помилку (Bug Report). Чому важливий пункт «Steps to Reproduce»?
2. Які особливості тестування об'єктно-орієнтованого коду (класів, наслідування)?
3. Що таке «Shift Left Testing» і чому цей підхід набуває популярності?
4. **Завдання:** У файлі `main.py` створіть функцію `validate_email(email: str)`, яка примітивно перевіряє наявність символу "@" та крапки після нього.
**Тест:** У файлі `test.py` перевірте валідні та невалідні формати email.

---
#### Білет №21
1. Що таке конфігураційне тестування і навіщо воно потрібне?
2. Як ізолювати тести від зовнішніх залежностей (бази даних, API)?
3. Опишіть поняття рефакторингу коду. Яку роль відіграють тести під час рефакторингу?
4. **Завдання:** У файлі `main.py` напишіть функцію `sort_list(numbers: list, reverse=False)`, яка сортує список.
**Тест:** У файлі `test.py` перевірте сортування за зростанням та спаданням.

---
#### Білет №22
1. Що таке Exploratory Testing (Дослідницьке тестування)? Коли його варто застосовувати?
2. Як перевірити тип поверненого значення функції у Unit-тесті?
3. Що таке «Test Suite» (Набір тестів) і як його сформувати в Python?
4. **Завдання:** У файлі `main.py` створіть функцію `get_file_extension(filename: str)`, яка повертає розширення файлу (частину після останньої крапки).
**Тест:** У файлі `test.py` перевірте файли з розширенням, без розширення та приховані файли (починаються з крапки).

---
#### Білет №23
1. Поясніть концепцію «Testing Pyramid» (Піраміда тестування). Яких тестів має бути найбільше?
2. Як ігнорувати певні тести при запуску (наприклад, ті, що ще не реалізовані)?
3. Що таке «Soft Assertions» і чим вони відрізняються від «Hard Assertions»?
4. **Завдання:** У файлі `main.py` напишіть функцію `triangle_area(base, height)`, яка обчислює площу трикутника.
**Тест:** У файлі `test.py` перевірте коректність формули та обробку нульових значень.

---
#### Білет №24
1. Що таке usability тестування (тестування зручності використання)?
2. Як працювати з файловою системою під час тестів (створення тимчасових файлів)?
3. Опишіть основні етапи процесу тестування (Test Process).
4. **Завдання:** У файлі `main.py` створіть функцію `filter_positive(numbers: list)`, яка повертає новий список тільки з додатними числами.
**Тест:** У файлі `test.py` протестуйте змішаний список, список тільки з від’ємних чисел та порожній список.

---
#### Білет №25
1. Що таке «Ad-hoc» тестування? Чим воно відрізняється від дослідницького?
2. Як перевірити, що дві змінні посилаються на один і той самий об'єкт (`assertIs`)?
3. Які ризики виникають при недостатньому тестуванні програмного забезпечення?
4. **Завдання:** У файлі `main.py` напишіть функцію `seconds_to_minutes(seconds: int)`, яка повертає рядок у форматі "mm:ss".
**Тест:** У файлі `test.py` перевірте значення менше хвилини, рівно хвилину та більше хвилини.

---
#### Білет №26
1. Що таке A/B тестування і чи стосується воно QA-інженерів?
2. Опишіть призначення файлу `__init__.py` у пакетах тестів.
3. Як забезпечити незалежність Unit-тестів один від одного?
4. **Завдання:** У файлі `main.py` створіть функцію `count_words(sentence: str)`, яка повертає кількість слів у реченні.
**Тест:** У файлі `test.py` перевірте речення з кількома пробілами між словами.

---
#### Білет №27
1. Що таке тести на проникнення (Penetration Testing) і до якого виду тестування вони належать?
2. Як перевірити правильність роботи з плаваючою крапкою (float), враховуючи похибку (`assertAlmostEqual`)?
3. Які дані повинні міститися в Test Plan (План тестування)?
4. **Завдання:** У файлі `main.py` напишіть функцію `bmi_calculator(weight, height)`, яка обчислює індекс маси тіла.
**Тест:** У файлі `test.py` перевірте коректність розрахунку для стандартних значень.

---
#### Білет №28
1. Що таке локалізаційне тестування (Localization Testing)?
2. Як використовувати `subTest` у `unittest` для перевірки кількох умов в одному тесті?
3. Поясніть поняття «Severity» (Серйозність) та «Priority» (Пріоритет) дефекту.
4. **Завдання:** У файлі `main.py` створіть клас `BankAccount` з методами `deposit` та `withdraw`.
**Тест:** У файлі `test.py` протестуйте, що баланс змінюється коректно і не можна зняти більше, ніж є на рахунку.

---
#### Білет №29
1. Що таке сумісність (Compatibility Testing)? Як її перевіряють?
2. Як організувати тестові дані? Чи варто хардкодити дані всередині тестів?
3. Для чого використовується ключове слово `with` при тестуванні виключень (`assertRaises`)?
4. **Завдання:** У файлі `main.py` напишіть функцію `generate_acronym(phrase: str)`, яка створює абревіатуру з перших літер слів (наприклад, "Unit Test" -> "UT").
**Тест:** У файлі `test.py` перевірте фрази різної довжини.

---
#### Білет №30
1. Що таке «Monkey Testing»?
2. Як бібліотека Coverage визначає, які рядки коду були виконані під час тесту?
3. Опишіть переваги використання Docker контейнерів для запуску тестів.
4. **Завдання:** У файлі `main.py` створіть функцію `find_duplicate(numbers: list)`, яка повертає перший знайдений дублікат або `None`.
**Тест:** У файлі `test.py` перевірте список без дублікатів та список з дублікатами.

---