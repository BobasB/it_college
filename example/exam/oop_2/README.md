# Завдання для ООП (Об'єктно-орієнтоване програмування)

## Загальна структура та вимоги завдання
- завдання виконується у окремій папці, яка є вашим проектом
- ініціалізується проект за допомогою **pipenv** або **poetry** (на ваш вибір)
- у проект встановлюються бібліотеки для роботи з AI (`google-adk`, `python-dotenv` та інші, які ви вважаєте за потрібне)
- створюється файл `README.md` де вказується номер варіанту та завдання
- активовується проект та створюється AI агент за допомогою засобів Google ADK
- **ЗАГАЛЬНЕ ЗАВДАННЯ:**
    * у файлі `agent.py` реалізувати OOP-класи, що демонструють **усі 4 парадигми ООП** (інкапсуляція, наслідування, поліморфізм, абстракція)
    * модифікувати промпт для AI агента згідно з варіантом завдання
    * створити інструмент (функцію `tool`), який буде використовувати AI агент згідно з варіантом завдання
- запустити проект та продемонструвати роботу AI агента, вивести результат
- якщо код працює коректно, завдання вважається виконаним

---
## Загальні вимоги до здачі

1. Проект знаходиться у окремій папці (наприклад `exam_variant_1/`)
2. Проект ініціалізований за допомогою `pipenv` або `poetry`
3. Встановлено залежності: `google-adk`, `python-dotenv`
4. Наявний файл `.gitignore` (ігноруються `.env`, папки середовища)
5. Наявний файл `README.md` з номером варіанту та описом завдання
6. Файл `agent.py` містить:
   - OOP-класи (всі 4 парадигми)
   - функцію-інструмент (tool)
   - визначення `root_agent`
7. Проект запускається командою `adk run` або `adk web`
8. У звіті наявний скріншот або лог роботи агента (мінімум 3 діалоги)
---
> **Критерії оцінювання:**
> - Коректна ініціалізація проекту з менеджером пакетів (pipenv або poetry)
> - Наявність `.gitignore` з ігноруванням файлів середовища та `.env`
> - Реалізація всіх 4 парадигм ООП у коді
> - Коректно визначений та підключений інструмент (tool) для агента
> - Змістовний промпт агента відповідно до варіанту
> - Демонстрація роботи агента (скріншот або лог діалогу у звіті)

---
> [!NOTE]
> Демонстраційний варіант 0 — Агент служби доставки *(зразок виконання)*
> Цей варіант є готовим прикладом того, як має бути виконане завдання. Повна реалізація знаходиться у папці [`demo/`](demo/).
> ### OOP-завдання
> Реалізуйте ієрархію класів для служби доставки посилок:
> 
> 1. Абстрактний клас `Package` з атрибутами `weight_kg: float`, `distance_km: float` та > абстрактним методом `shipping_cost() -> float`.
> 2. Класи `StandardPackage` та `ExpressPackage` що успадковують `Package` та реалізують > `shipping_cost()`:
>    - `StandardPackage` має приватний атрибут `__rate_per_kg = 5.0` (інкапсуляція), > властивість-геттер `rate`, формула: `weight_kg * rate + distance_km * 0.2`
>    - `ExpressPackage` має приватний `__rate_per_kg = 10.0` та класовий атрибут `SURCHARGE > = 50.0`, формула: `weight_kg * rate + distance_km * 0.5 + SURCHARGE`
> 3. Клас `DeliveryService` що:
>    - зберігає замовлення у приватному списку `__orders: list[Package]` (інкапсуляція)
>    - методи `add_order(package)`, `total_revenue() -> float`, `order_count() -> int`
> 
> ### AI-агент
> - **Інструмент (tool):** функція `get_delivery_cost(package_type: str, weight_kg: float, distance_km: float) -> dict` — **всередині функції** створює об'єкт `DeliveryService`, на > основі `package_type` створює `StandardPackage` або `ExpressPackage`, додає через > `add_order()`, отримує вартість через `shipping_cost()` та повертає `{"package_type": ..., > "weight_kg": ..., "distance_km": ..., "rate_per_kg_uah": ..., "cost_uah": ..., > "description": ...}`.
> - **Промпт агента:** агент є помічником служби доставки "Nova Post". Він розраховує > вартість доставки посилок двох типів (стандартна/експрес) та пояснює з чого складається > ціна. Відповідає **українською мовою**.
> - **Демонстрація:** поставте агенту 3 запитання з різними типами посилок, вагами та > відстанями.

---

## Варіант 1 — Агент прогнозу погоди

### OOP-завдання
Реалізуйте ієрархію класів для метеостанції:

1. Абстрактний клас `Sensor` (модуль `abc`) з абстрактним методом `read()` та атрибутами `name` і `unit`.
2. Класи `TemperatureSensor` та `HumiditySensor`, що успадковують `Sensor` та реалізують метод `read()` — повертають відповідне числове значення (температура в °C, вологість у %). Значення можна задати через `random`.
3. Клас `WeatherStation` що:
   - приймає `city: str` у конструкторі
   - зберігає список датчиків (`__sensors` — приватний атрибут, інкапсуляція)
   - має метод `add_sensor(sensor)` та метод `report() -> dict` — повертає словник `{"city": ..., "temperature_c": ..., "humidity_percent": ..., "condition": ...}` зібраний з показників датчиків (поліморфізм через виклик `read()` кожного датчика)

### AI-агент
- **Інструмент (tool):** функція `get_weather(city: str) -> dict` — **всередині функції** створює об'єкт `WeatherStation(city)`, додає до нього `TemperatureSensor` та `HumiditySensor`, викликає метод `report()` та повертає результат.
- **Промпт агента:** агент є метеорологічним помічником. Він повідомляє про поточну погоду в містах та дає рекомендації (що вдягнути, чи брати парасолю тощо). Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання про погоду в різних містах.

### Теоретичні питання

1. Поясніть суть принципу інкапсуляції та детально опишіть, як він реалізується в мові Python за допомогою модифікаторів доступу (рівнів захищеності атрибутів) та декораторів.
2. Що таке контекстні менеджери в Python і для чого вони використовуються? Опишіть, як створити власний контекстний менеджер на основі класу, та які спеціальні методи для цього необхідно перевизначити.
3. Опишіть загальну суть SOLID принципів в об'єктно-орієнтованому проектуванні. Оберіть один із цих п'яти принципів на власний розсуд і розгорнуто поясніть його архітектурну цінність.

---

## Варіант 2 — Агент розрахунку геометричних фігур

### OOP-завдання
Реалізуйте ієрархію геометричних фігур:

1. Абстрактний клас `Shape` з абстрактними методами `area()` та `perimeter()`.
2. Класи `Rectangle`, `Triangle`, `Circle` що успадковують `Shape` та реалізують обидва методи.
3. Клас `ShapeStorage` що:
   - зберігає список фігур (`__shapes` — приватний атрибут, інкапсуляція)
   - має методи `add(shape)` та `total_area()` — повертає суму площ усіх фігур (поліморфізм)

### AI-агент
- **Інструмент (tool):** функція `calculate_shape_area(shape: str, params: dict) -> dict` — приймає назву фігури (`"rectangle"`, `"triangle"`, `"circle"`) та словник з параметрами (наприклад, `{"width": 5, "height": 3}`). **Всередині функції** на основі `shape` створюється відповідний об'єкт (`Rectangle`, `Triangle` або `Circle`) з переданими параметрами, після чого викликаються методи `area()` та `perimeter()`. Повертає `{"shape": ..., "area": ..., "perimeter": ...}`.
- **Промпт агента:** агент є математичним асистентом з геометрії. Він обчислює площі та периметри фігур за введеними розмірами та пояснює формули. Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 задачі з геометрії (різні фігури та розміри).

### Теоретичні питання

1. Що таке спеціальні (магічні) методи класів у Python? Наведіть приклади використання та призначення методів `__init__` та `__str__`, обґрунтувавши їхню роль у життєвому циклі об'єкта.
2. Поясніть різницю між концепціями ітераторів та генераторів у Python. Яким чином проектування та створення ітераторів інтегрується в об'єктно-орієнтований підхід?
3. Що таке патерни (шаблони) проектування ПЗ? Поясніть призначення, структуру та практичний принцип реалізації патерна "Singleton" (Одинак) у Python.

---

## Варіант 3 — Агент управління магазином

### OOP-завдання
Реалізуйте систему товарів магазину:

1. Абстрактний клас `Product` з атрибутами `name`, `price: float`, `quantity: int` та абстрактним методом `get_info() -> dict`.
2. Класи `Electronics` та `FoodItem` що успадковують `Product` та реалізують `get_info()` — повертають словник з інформацією про товар у різному форматі (наприклад, `Electronics` додає поле `"warranty_years"`, а `FoodItem` — `"expiry_days"`).
3. Клас `Store` що:
   - зберігає товари у приватному словнику `__inventory: dict[str, Product]` (інкапсуляція)
   - має методи `add_product(product)`, `find(name: str) -> Product | None` та `list_products()` (поліморфізм через `get_info()`)

### AI-агент
- **Інструмент (tool):** функція `get_product_price(product_name: str) -> dict` — **всередині функції** створює об'єкт `Store`, додає кілька заздалегідь визначених товарів (`Electronics`, `FoodItem`), викликає `find(product_name)`, отримує інформацію через `get_info()` та повертає її. Якщо товар не знайдено — повертає `{"available": False}`.
- **Промпт агента:** агент є консультантом інтернет-магазину. Він повідомляє ціни на товари, наявність та деталі (гарантія, термін придатності). Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання про різні товари.

### Теоретичні питання

1. Опишіть різницю між простим та множинним успадкуванням класів. Які архітектурні проблеми (наприклад, "проблема ромба") можуть виникнути при множинному успадкуванні та як працює механізм MRO (Method Resolution Order) у Python.
2. Що таке метапрограмування в Python? Поясніть призначення метакласів та наведіть приклад ситуації, коли доцільно використовувати метакласи для динамічного створення чи модифікації класів.
3. Поясніть призначення та архітектурну роль патернів проектування "Factory" (Фабрика) та "Observer" (Спостерігач) в об'єктно-орієнтованих програмах.

---

## Варіант 4 — Агент успішності студентів

### OOP-завдання
Реалізуйте систему обліку успішності:

1. Абстрактний клас `Person` з атрибутами `name`, `age` та абстрактним методом `get_role() -> str`.
2. Клас `Student` що успадковує `Person`:
   - зберігає оцінки у приватному списку `__grades` (інкапсуляція)
   - методи `add_grade(grade: float)`, `average() -> float`, `min_grade() -> float`, `max_grade() -> float`, `get_role()`
3. Клас `Teacher` що успадковує `Person`:
   - атрибут `subject`
   - методи `get_role()` та `evaluate(student: Student, grade: float)` — додає оцінку студенту через `student.add_grade()`

### AI-агент
- **Інструмент (tool):** функція `calculate_grade(name: str, scores: list) -> dict` — **всередині функції** створює об'єкт `Student(name, age=18)`, додає кожну оцінку зі списку через `add_grade()`, після чого викликає `average()`, `min_grade()`, `max_grade()` для формування відповіді. `letter_grade` визначається за шкалою: 90+ → A, 75+ → B, 60+ → C, нижче → F. Повертає `{"student": ..., "average": ..., "min": ..., "max": ..., "letter_grade": ...}`.
- **Промпт агента:** агент є освітнім асистентом. Він розраховує середній бал, визначає рейтинг та дає поради щодо покращення успішності. Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання з іменами студентів та різними наборами оцінок.

### Теоретичні питання

1. Що таке міксини (Mixins) в об'єктно-орієнтованому програмуванні? Поясніть, як за допомогою класів-модифікаторів реалізується концепція створення багаторазового коду в мові Python.
2. Як реалізується асинхронне програмування в рамках ООП в Python? Опишіть особливості та переваги використання асинхронних функцій, методів та об'єктів для оптимізації виконання завдань.
3. Що таке модульне тестування (Unit Testing) та концепція Mocking? Чому важливо тестувати об'єкти та їхні методи ізольовано від зовнішніх залежностей за допомогою бібліотек `unittest` та `mock`?

---

## Варіант 5 — Агент конвертації валют

### OOP-завдання
Реалізуйте систему фінансових активів:

1. Абстрактний клас `Asset` з атрибутами `name`, `amount` та абстрактним методом `get_value_uah() -> float`.
2. Клас `CurrencyAsset` що успадковує `Asset`:
   - приватний атрибут `__rate_to_uah` (інкапсуляція)
   - властивість-геттер `rate` та метод `get_value_uah()` — повертає `self.amount * self.__rate_to_uah`
3. Клас `CryptoAsset` що успадковує `Asset`:
   - власна логіка `get_value_uah()` з волатильністю: базовий курс + `randint(-500, 500)`
4. Клас `Portfolio` що:
   - зберігає список активів (`__assets` — приватний, інкапсуляція)
   - методи `add(asset)` та `total_value_uah()` — сума вартості всіх активів (поліморфізм через `get_value_uah()`)

### AI-агент
- **Інструмент (tool):** функція `convert_currency(amount: float, from_currency: str, to_currency: str) -> dict` — **всередині функції** на основі фіксованих курсів до UAH (USD → 41.5, EUR → 45.0, GBP → 52.0, BTC → 3_800_000) створює об'єкт `CurrencyAsset(from_currency, amount, rate_to_uah)`, викликає `get_value_uah()` щоб отримати суму в UAH, потім ділить на курс цільової валюти. Повертає `{"from": ..., "to": ..., "amount": ..., "result": ..., "rate": ...}`.
- **Промпт агента:** агент є фінансовим консультантом з обміну валют. Він конвертує суми між валютами та пояснює поточний курс. Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання з конвертації різних валют.

### Теоретичні питання

1. Порівняйте принцип композиції та принцип успадкування в ООП. У яких випадках і з яких причин розробнику варто надавати перевагу саме композиції чи агрегації об'єктів перед успадкуванням.
2. Опишіть системні підходи до обробки винятків (exceptions), логування подій (logging) та перенаправлення інтерфейсів вводу/виводу в об'єктно-орієнтованих програмах на Python.
3. Поясніть поняття рефакторингу об'єктно-орієнтованого коду. Які основні ознаки "поганого коду" (code smells) сигналізують про необхідність рефакторингу для підвищення ефективності та масштабованості системи.

---

## Варіант 6 — Агент бібліотечного каталогу

### OOP-завдання
Реалізуйте систему обліку книг у бібліотеці:

1. Абстрактний клас `LibraryItem` з атрибутами `title`, `year: int` та абстрактним методом `get_info() -> dict`.
2. Класи `Book` та `Magazine` що успадковують `LibraryItem` та реалізують `get_info()`:
   - `Book` додає поля `author` та `pages: int`
   - `Magazine` додає поля `issue: int` та `topic`
3. Клас `Library` що:
   - зберігає елементи у приватному списку `__catalog` (інкапсуляція)
   - методи `add(item)`, `find(title: str) -> LibraryItem | None` та `list_all()` (поліморфізм через `get_info()`)

### AI-агент
- **Інструмент (tool):** функція `search_book(title: str) -> dict` — **всередині функції** створює об'єкт `Library`, наповнює його заздалегідь визначеними об'єктами `Book` та `Magazine`, викликає `find(title)`, отримує дані через `get_info()` та повертає їх. Якщо не знайдено — повертає `{"found": False}`.
- **Промпт агента:** агент є бібліотечним помічником. Він шукає книги та журнали за назвою та надає інформацію про них. Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання про пошук різних видань.

### Теоретичні питання

1. Поясніть призначення, синтаксис та практичну користь використання декоратора `@property`, а також розкрийте різницю між поняттями "атрибути класу", "атрибути екземпляра" та "властивості" у Python.
2. Що таке серіалізація та десеріалізація об'єктів? Порівняйте особливості, сфери застосування, переваги та недоліки роботи з форматами `pickle` та `json` у Python.
3. Навіщо використовуються UML-діаграми класів у проектуванні програмного забезпечення? Опишіть основні типи взаємозв'язків між класами, які можна наочно відобразити на таких діаграмах.

---

## Варіант 7 — Агент фітнес-тренера

### OOP-завдання
Реалізуйте систему обліку тренувань:

1. Абстрактний клас `Exercise` з атрибутами `name`, `duration_min: int` та абстрактним методом `calories_burned() -> float`.
2. Класи `CardioExercise` та `StrengthExercise` що успадковують `Exercise` та реалізують `calories_burned()`:
   - `CardioExercise` має коефіцієнт `intensity` (1.0–2.0), формула: `duration_min * 8 * intensity`
   - `StrengthExercise` має `weight_kg: float`, формула: `duration_min * 5 + weight_kg * 0.5`
3. Клас `Workout` що:
   - зберігає список вправ (`__exercises` — приватний, інкапсуляція)
   - методи `add(exercise)` та `total_calories() -> float` (поліморфізм через `calories_burned()`)
   - метод `summary() -> dict` — повертає `{"exercises": [...], "total_calories": ...}`

### AI-агент
- **Інструмент (tool):** функція `calculate_workout(exercises: list) -> dict` — **всередині функції** створює об'єкт `Workout`, для кожного елементу зі списку (словник з полями `type`, `name`, `duration_min` та `intensity`/`weight_kg`) створює відповідний об'єкт `CardioExercise` або `StrengthExercise` і додає через `add()`, потім викликає `summary()` та повертає результат.
- **Промпт агента:** агент є персональним фітнес-тренером. Він розраховує калорії для тренувань та дає рекомендації щодо навантаження. Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання з різними наборами вправ.

### Теоретичні питання

1. Що таке абстрагування та як воно пов'язане з поліморфізмом? Поясніть призначення абстрактних класів та інтерфейсів, а також особливості їхньої реалізації в мові Python за допомогою модуля `abc`.
2. Опишіть особливості роботи з базами даних (наприклад, SQLite) та системами кешування (наприклад, Redis) в контексті об'єктно-орієнтованих Python-додатків.
3. Опишіть архітектурні особливості веб-фреймворку Django (шаблон MVT/MVC). Як використання вбудованих класів цього фреймворку допомагає автоматизувати роботу з веб-застосунками?

---

## Варіант 8 — Агент розкладу транспорту

### OOP-завдання
Реалізуйте систему розкладу громадського транспорту:

1. Абстрактний клас `Transport` з атрибутами `route_number`, `departure: str` та абстрактним методом `get_schedule() -> dict`.
2. Класи `Bus` та `Train` що успадковують `Transport` та реалізують `get_schedule()`:
   - `Bus` додає `stops: list[str]` — список зупинок
   - `Train` додає `stations: list[str]` та `travel_time_min: int`
3. Клас `Schedule` що:
   - зберігає маршрути у приватному словнику `__routes: dict[str, Transport]` (інкапсуляція)
   - методи `add_route(transport)`, `find_route(route_number: str) -> Transport | None` та `list_routes()` (поліморфізм через `get_schedule()`)

### AI-агент
- **Інструмент (tool):** функція `get_transport_schedule(route_number: str) -> dict` — **всередині функції** створює об'єкт `Schedule`, наповнює його заздалегідь визначеними маршрутами `Bus` та `Train`, викликає `find_route(route_number)`, отримує дані через `get_schedule()` та повертає їх. Якщо маршрут не знайдено — повертає `{"found": False}`.
- **Промпт агента:** агент є помічником з громадського транспорту. Він надає інформацію про маршрути, зупинки та час у дорозі. Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання про різні маршрути.

### Теоретичні питання

1. Поясніть різницю між атрибутами класу (статичними даними) та атрибутами екземпляра класу в Python. Наведіть приклад практичного сценарію, коли використання атрибутів саме на рівні класу є необхідним.
2. Що таке ізольовані віртуальні середовища (virtual environments) в Python? Чому менеджмент залежностей та робота у віртуальних середовищах є критично важливими при розробці великих об'єктно-орієнтованих проектів?
3. Поясніть процес створення власних об'єктно-орієнтованих бібліотек у Python та особливості їхнього менеджменту й публікації на платформі open-source пакетів PYPI.ORG.

---

## Варіант 9 — Агент рецептів страв

### OOP-завдання
Реалізуйте систему рецептів:

1. Абстрактний клас `Dish` з атрибутами `name`, `cooking_time_min: int` та абстрактним методом `get_nutrition() -> dict`.
2. Класи `VeganDish` та `MeatDish` що успадковують `Dish` та реалізують `get_nutrition()`:
   - `VeganDish` має атрибут `calories: int` — повертає `{"calories": ..., "type": "vegan"}`
   - `MeatDish` має атрибути `calories: int` та `protein_g: float` — повертає `{"calories": ..., "protein_g": ..., "type": "meat"}`
3. Клас `CookBook` що:
   - зберігає рецепти у приватному словнику `__recipes: dict[str, Dish]` (інкапсуляція)
   - методи `add(dish)`, `find(name: str) -> Dish | None` та `list_all()` (поліморфізм через `get_nutrition()`)

### AI-агент
- **Інструмент (tool):** функція `get_recipe_info(dish_name: str) -> dict` — **всередині функції** створює об'єкт `CookBook`, наповнює його заздалегідь визначеними стравами `VeganDish` та `MeatDish`, викликає `find(dish_name)`, отримує харчову цінність через `get_nutrition()` та повертає її разом із `cooking_time_min`. Якщо страва не знайдена — повертає `{"found": False}`.
- **Промпт агента:** агент є кулінарним помічником. Він надає інформацію про рецепти, калорійність та тип страви, а також дає рекомендації щодо харчування. Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання про різні страви.

### Теоретичні питання

1. Поясніть концепцію перевантаження операторів (operator overloading) в ООП. Опишіть, як воно реалізується в Python для користувацьких класів за допомогою магічних методів (на прикладі математичного додавання або порівняння).
2. Поясніть концепцію автоматизації тестування та розгортання ПЗ за допомогою інструменту GitHub Actions (CI/CD). Яку роль вона відіграє у сучасному життєвому циклі розробки програмних продуктів?
3. Що таке автоматизоване створення документації для об'єктно-орієнтованого коду в Python? Поясніть роль рядків документації (docstrings) та інструментів генерації описів класів і їхніх методів.

---

## Варіант 10 — Агент паркінгу

### OOP-завдання
Реалізуйте систему управління паркінгом:

1. Абстрактний клас `Vehicle` з атрибутами `plate: str`, `owner: str` та абстрактним методом `parking_rate() -> float` (вартість за годину, грн).
2. Класи `Car` та `Truck` що успадковують `Vehicle` та реалізують `parking_rate()`:
   - `Car`: фіксована ставка 30 грн/год
   - `Truck` має атрибут `weight_tons: float`, ставка: `50 + weight_tons * 5` грн/год
3. Клас `ParkingLot` що:
   - зберігає припарковані авто у приватному словнику `__parked: dict[str, tuple[Vehicle, float]]` де значення — `(vehicle, entry_hour)` (інкапсуляція)
   - методи `park(vehicle, entry_hour: float)`, `leave(plate: str, exit_hour: float) -> dict` — розраховує суму через `parking_rate()` і час стоянки (поліморфізм), та `list_parked()` — виводить список авто

### AI-агент
- **Інструмент (tool):** функція `calculate_parking_cost(vehicle_type: str, hours: float, weight_tons: float = 0) -> dict` — **всередині функції** створює об'єкт `Car` або `Truck` (залежно від `vehicle_type`) з тимчасовим номером, викликає `parking_rate()` щоб отримати ставку, розраховує `cost = parking_rate() * hours` та повертає `{"vehicle_type": ..., "hours": ..., "rate_per_hour": ..., "total_cost": ...}`.
- **Промпт агента:** агент є оператором паркінгу. Він розраховує вартість стоянки для різних типів транспортних засобів. Відповідає **українською мовою**.
- **Демонстрація:** поставте агенту 3 запитання з різними типами авто та часом стоянки.

### Теоретичні питання

1. Поясніть поняття "перевизначення методів" (method overriding) та "розширення класів". Чим перевизначення методів концептуально відрізняється від перевантаження методів (method overloading)?
2. Що таке системи контролю версій (наприклад, Git) та платформа GitHub? Опишіть базові поняття комітів (commits), гілок (branches) та синхронізації локального робочого простору в IDE з віддаленим репозиторієм.
3. Опишіть роль та практичне призначення файлів README та форматування Markdown при веденні документації, оформленні звітів та представленні структури об'єктно-орієнтованого проекту.

---
